# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# Node / frontend
frontend/node_modules/
frontend/node_modules/.tmp/
frontend/node_modules/.vite-temp/

# Build outputs
build/
build-protected/
dist/
dist-protected/
EndSolMacro-protected.exe
*.spec.bak

# Frontend dist is produced by npm run build (keep it only inside release EXEs)
frontend/dist/

# Local runtime data and diagnostics
logs/
logs/memory_match/
*.log

# Secrets / personal config — NEVER commit
config.json
config_folder/
*.local.json

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/
# ── Development-only files — never public ──────────────────────
publish_github.bat
AGENTS.md
*.zip
.env
desktop.ini

# Local runtime data that must never leak into the repo
node_modules/
media_cache/
custom_paths/
Calibrations/
profiles/
images/
multi_instances.json
config.backup-*.json
config.json.bak*
config.json.before_restore
config.json.migration_backup
*.preserve
